- name: Brave Browser
  homebrew_cask:
    name: brave-browser

- name: Zen Browser
  homebrew_cask:
    name: zen

- name: Record role in lockfile
  command: "{{ playbook_dir }}/bin/lock-role {{ ansible_role_name }}"
  changed_when: false
